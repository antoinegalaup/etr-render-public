{
  "name": "etr-render-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Deploy-focused mirror for the ETR API and worker services",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "api": "node src/server.js",
    "api:dev": "npm --prefix apps/api run dev",
    "api:start": "npm --prefix apps/api run start",
    "workers:start": "npm --prefix apps/workers run start",
    "workers:once": "npm --prefix apps/workers run once"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "commander": "^12.1.0",
    "express": "^4.21.2",
    "mysql2": "^3.17.0",
    "pg": "^8.18.0"
  }
}
